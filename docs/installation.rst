============
Installation
============

At the command line::

    $ easy_install django-diagnostic

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-diagnostic
    $ pip install django-diagnostic
